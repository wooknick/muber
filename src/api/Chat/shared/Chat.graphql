type Chat {
    id: Int!
    messages: [Message]!
    rideId: Int!
    ride: Ride!
    passengerId: Int!
    passenger: User!
    driverId: Int!
    driver: User!
    createdAt: String!
    updatedAt: String
}
